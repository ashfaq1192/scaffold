# Specification: Greeting CLI

## Requirements
1. Build a command-line interface tool in `cli.py` using the Python `click` library.
2. The CLI must take an optional or required `--name` parameter (string).
3. When executed, it must print a greeting message formatted as: `hi <name>`.
4. It must include a helpful description when `--help` is passed.